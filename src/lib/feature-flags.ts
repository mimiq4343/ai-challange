/**
 * Версионируемые переключатели поведения. Переменные окружения остаются только
 * для секретов и адресов, поэтому функциональные флаги живут здесь.
 *
 * `personalization` — профиль пользователя Day 12: блок профиля в промпте,
 * автоматическое обучение предпочтений и API `/api/profiles`. Выключение не
 * удаляет ни код, ни таблицы: долговременная память Day 11 привязана к профилю
 * и продолжает работать на профиле по умолчанию. Возврат — `true`.
 */
export const FEATURES = {
  personalization: false,
} as const;

export const PERSONALIZATION_DISABLED_MESSAGE =
  "Персонализация отключена флагом personalization в src/lib/feature-flags.ts.";
